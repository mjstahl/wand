uses {IO}

import Stream
import Map
import IO

-- Tally how many times each log level appears.
--
-- The reducer is the part worth thinking about, so leave it as a hole (?)
-- and ask the type system what belongs there before writing it.
let levels = Stream.fold_left ? Map.empty (IO.stdin_lines ())

levels
