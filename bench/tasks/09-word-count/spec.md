Write a wand function `word_count : List String -> Map Int` that takes a
list of words and returns a map from each distinct word to the number of
times it appears. Use the stdlib `Map` module (`import Map`).

Write only the function definition(s) needed (plus the `import`) — no
example calls, no top-level script.
