Define a wand type `Shape` with two variants:
- `Circle`, carrying one `Int` radius
- `Rect`, carrying two `Int` fields (width and height)

Write a function `area : Shape -> Int` that returns the area. Use
`3` as an approximation of pi for the circle case (i.e. `r * r * 3`).

Write only the type definition and function definition(s) needed — no
example calls, no top-level script.
