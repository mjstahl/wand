type Stack 'a = Stack (List 'a)

let empty = Stack []

let push x (Stack xs) = Stack (x : xs)

let pop (Stack []) = Error "pop: empty stack"
let pop (Stack [h : t]) = Ok (h, Stack t)

let peek (Stack []) = Error "peek: empty stack"
let peek (Stack [h : _]) = Ok h
