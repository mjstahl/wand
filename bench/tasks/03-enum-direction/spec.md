Define a wand type `Direction` with four no-payload variants: `North`,
`South`, `East`, `West`.

Write a function `opposite : Direction -> Direction` that returns the
opposite direction (`North`<->`South`, `East`<->`West`).

Write only the type definition and function definition(s) needed — no
example calls, no top-level script.
