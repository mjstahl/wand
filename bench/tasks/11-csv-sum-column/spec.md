Write a wand function `sum_column : Int -> String -> Int` that parses its
second argument as CSV (comma-separated, using the stdlib `CSV` module,
`import CSV`) and sums the numeric values in the column at the given
0-indexed position, skipping the first row (the header). Assume every
non-header row has a valid integer in that column.

Write only the function definition(s) needed (plus imports) — no example
calls, no top-level script.
