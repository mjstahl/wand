import CSV

let sum_column idx csv_text =
  let rows = CSV.parse csv_text in
  let data = List.tail rows in
  let values = List.map (fun row -> String.to_int (List.get! idx row)) data in
  List.fold_left (fun acc r ->
    match r with
    | Ok n -> acc + n
    | Error _ -> acc) 0 values
