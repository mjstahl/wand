Write the following, using explicit `(Int, Int)` tuple type annotations
(not just relying on inference) in both places:

1. A value `origin : (Int, Int)` bound to `(0, 0)`.
2. A function `translate` that takes two 2D points, each represented as an
   `(Int, Int)` tuple, and returns their component-wise sum as an
   `(Int, Int)` tuple — annotate its return type explicitly.

Write only these two definitions — no example calls, no top-level script.
