Write a wand function `sum_evens : List Int -> Int` that returns the sum
of the even numbers in the given list, using recursion (no stdlib `List`
import). An empty list sums to `0`.

Write only the function definition(s) needed — no example calls, no
top-level script.
